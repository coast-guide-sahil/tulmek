/**
 * TULMEK Core Ranking Algorithm — re-exported from @tulmek/core.
 * The algorithm lives in packages/core/src/domain/ranking.ts
 * so it can be shared across web, mobile, and desktop.
 */
export { tulmekRank } from "@tulmek/core/domain";
