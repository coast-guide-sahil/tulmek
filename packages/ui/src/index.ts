/**
 * @tulmek/ui — Shared UI primitives and tokens.
 *
 * Platform-agnostic types, theme colors, and utilities
 * used by web (Next.js), desktop (Tauri), and mobile (Expo/RN).
 *
 * Add once here → available on all platforms.
 */
export { lightTheme, darkTheme, categoryAccents } from "./types";
export type { ThemeColors } from "./types";
